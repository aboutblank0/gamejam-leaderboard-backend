services:
  db:
    image: postgres:16
    restart: always
    
    environment:
      POSTGRES_PASSWORD: <YOUR PASSWORD>

    volumes:
      - pgdata:/var/lib/postgresql/data

  adminer:
    image: adminer
    restart: always

    ports:
      - 8080:8080

volumes:
  pgdata:
